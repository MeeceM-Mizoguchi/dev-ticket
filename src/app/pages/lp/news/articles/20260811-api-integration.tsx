import { ScreenFigure, ConceptFigure, ApiIntegrationScreen, ApiFlowDiagram } from './screens';

/**
 * 記事本文。プレーンな HTML を書くだけで共通タイポグラフィが適用されます。
 * 画面イメージは実機能をトレース:
 *  ・ApiIntegrationDialog.tsx（「使い方」「APIキー」の2タブ・発行直後の平文表示）
 *  ・CreateTicketMenu.tsx（新規チケットの4項目メニュー）
 *  ・api/v1/[resource].ts（POST /api/v1/tickets ・ GET /api/v1/context）
 * ENHA2-043 とその分割チケット（043-1 / 043-2 / 043-3）PR #324 の告知。
 */
export default function ApiIntegration() {
  return (
    <>
      <p>
        Dev Ticket 専用の<strong>APIキー</strong>を発行し、
        <strong>外部のAIや自動化の仕組みから直接チケットを登録できる</strong>ようにしました。
        MDファイルを人が開いて取り込む工程そのものが不要になり、
        設計が固まった時点でチケットが並んでいる状態を作れます。
      </p>

      <ConceptFigure caption="キーはプロジェクト単位。そのキーで届くのは、紐付いたプロジェクトの中だけです">
        <ApiFlowDiagram />
      </ConceptFigure>

      <h2>1. APIキーは画面から発行できます</h2>
      <p>
        スプリント帯の「新規チケット」→<strong>「API連携」</strong>を開くと、
        <strong>「使い方」「APIキー」</strong>の2つのタブが表示されます。
        キーの発行・失効・棚卸しはすべてこの画面で完結し、<strong>発行は最初の1回だけ</strong>です。
        有効期限は30日・90日・1年・無期限から選べます。
      </p>

      <ScreenFigure label="API連携" caption="発行直後にだけ平文が表示されます。以降は先頭部分しか出ないので、その場でコピーしてください">
        <ApiIntegrationScreen />
      </ScreenFigure>

      <h2>2. キーの扱いは、パスワードと同じです</h2>
      <p>
        発行したキーの平文は<strong>その場で1回だけ表示され、Dev Ticket 側には保存されません</strong>。
        一覧に出るのは先頭部分だけです。
        失効させれば<strong>その時点から拒否</strong>されるので、
        漏れた疑いがあるときは新しいキーを発行して古いものを失効させてください。
      </p>
      <p>
        キーは<strong>プロジェクト単位</strong>で発行します。そのキーで登録できるのは紐付いたプロジェクトの中だけで、
        別のプロジェクトのスプリントを指定しても受け付けません。
        1本のキーにつき<strong>1分あたり60回</strong>までという上限も設けています。
      </p>

      <h2>3. 送る内容は、画面から作るときと同じです</h2>
      <p>
        <strong>POST /api/v1/tickets</strong> にスプリントとチケットの配列を送ると、
        <strong>親子1階層までまとめて登録</strong>され、作られたチケット番号が返ります。
        ステータス・優先度・分類・担当者・開始日・期限日・見積工数・本文（Markdown）が指定でき、
        <strong>画面から作った場合とまったく同じ形</strong>で登録されます。
      </p>
      <p>
        読み取れなかった値は<strong>空欄にしたうえで警告として返し、登録自体は止めません</strong>。
        たとえば存在しない担当者名を送った場合、担当者は空欄で登録され、その理由が応答に含まれます。
      </p>
      <p>
        あわせて <strong>GET /api/v1/context</strong> を用意しました。
        <strong>そのプロジェクトのスプリント・メンバー名・分類名・使えるステータスと優先度</strong>が返るので、
        AIやCIの側で「送ってよい値」を先に確認できます。
      </p>

      <h2>4. AIに渡す手順書もコピーできます</h2>
      <p>
        「使い方」タブには、<strong>そのスプリント向けの手順をそのままコピーできる</strong>ボタンがあります。
        エンドポイント・送るJSONの形・プロジェクトのメンバー名と分類名・本文の項目構成が
        1つのテキストにまとまっているので、コマンドを実行できるAIに貼るだけで動き始めます。
      </p>

      <h2>5. あわせて「新規チケット」ボタンを整理しました</h2>
      <p>
        スプリント帯に並んでいたボタンから<strong>「一括作成」を廃止</strong>し、
        <strong>「新規チケット」をクリックすると4つの項目のメニューが開く</strong>形に集約しました。
      </p>
      <ul>
        <li><strong>チケット作成</strong> … これまで通り1件ずつ入力して登録</li>
        <li><strong>MDファイルから取り込み</strong> … AIが書いたMDを取り込んで一括登録</li>
        <li><strong>一括作成</strong> … 表に直接入力してまとめて登録</li>
        <li><strong>API連携</strong> … AIやCIから直接登録できるようにする</li>
      </ul>
      <p>
        プランや権限で使えない項目は<strong>隠さずグレー表示にし、理由をその場に出します</strong>。
        「メニューに無いのはなぜか」を探しに行かなくて済むようにするためです。
      </p>
      <blockquote>
        ※ APIキーの発行は管理者のみ行えます。管理者以外の方には「API連携」がグレー表示になり、
        その旨が表示されます。
      </blockquote>

      <h2>主な特長</h2>
      <ul>
        <li>プロジェクト単位でAPIキーを発行。外部のAI・CIから直接チケットを登録できる</li>
        <li>キーの平文は発行直後の1回だけ表示。失効させればその場から拒否</li>
        <li>親子1階層までまとめて登録でき、作成されたチケット番号が返る</li>
        <li>読めなかった値は空欄＋警告で返し、登録自体は止めない</li>
        <li>スプリント・メンバー・分類の候補を取得する参照用のエンドポイントも用意</li>
        <li>AIに渡す手順書をワンクリックでコピー</li>
        <li>「新規チケット」を4項目のメニューへ集約。使えない項目は理由付きでグレー表示</li>
      </ul>

      <h2>ご利用方法</h2>
      <p>
        スプリント帯の「新規チケット」→「API連携」からご利用いただけます。
        APIキーの発行は管理者のみ行えますので、ご利用の際は管理者にご相談ください。
      </p>
    </>
  );
}
