// MDファイルからのチケット作成で使う、書き方の型（テンプレート）と AI 用プロンプト。
//
// テンプレートは1種類だけ。用途ごとに選ばせるより「全項目を埋めてもらう」1本に絞ったほうが
// 迷わず、取り込み後に画面側で直せばよいため。1本のなかに
// 「1作業で完結するチケット（##）」と「親＋子に分けるチケット（## + ###）」の
// 両方の例を入れてあり、どちらの書き方も同じテンプレートから出せるようにしている。
import { MD_STATUS_LABELS, MD_PRIORITY_LABELS } from "./parse";

/** そのまま .md にして書き足せる、全項目入りの記入例（フラット／親子の両方を含む） */
export const MD_TICKET_TEMPLATE = `## ログイン画面のバリデーション追加
- ステータス: 未着手
- 優先度: 高
- 分類: バグ
- 担当者: 山田太郎
- 開始日: 2026/08/03
- 期限日: 2026/08/10
- 見積工数: 4

**ASIS（現状）**
メールアドレス欄が未入力でも送信でき、サーバ側で500エラーになる。
形式が不正な場合もエラー内容が画面に表示されず、ユーザーは原因が分からない。

**TOBE（あるべき姿）**
送信前にメールアドレス形式をチェックし、不正な場合は入力欄の直下にエラーメッセージを表示する。
半角英数のみ許可し、全角は弾く。未入力のまま送信できない状態にする。

## 検索APIのレスポンス改善
- ステータス: 進行中
- 優先度: 中
- 分類: 仕様確認
- 担当者: 佐藤花子
- 開始日: 2026/08/04
- 期限日: 2026/08/18
- 見積工数: 8

**ASIS（現状）**
一覧取得でN+1クエリが発生しており、1000件表示に約8秒かかっている。
件数が増えるほど線形に悪化し、タイムアウトの報告も出ている。

**TOBE（あるべき姿）**
関連データを一括取得してN+1を解消し、1000件表示を1秒以内にする。
対象は以下の通り。

- 商品一覧エンドポイント
- ユーザー一覧エンドポイント
- 注文履歴エンドポイント

## ユーザー認証機能のリニューアル
- ステータス: 未着手
- 優先度: 高
- 担当者: 山田太郎
- 開始日: 2026/08/11
- 期限日: 2026/08/28
- 見積工数: 20

**ASIS（現状）**
ログイン周りが機能ごとに個別実装されており、パスワードリセットは未実装。
ログアウトしてもセッションが残るなど、認証まわりの挙動が統一されていない。

**TOBE（あるべき姿）**
ログイン・パスワードリセット・ログアウトを一式で作り直し、認証の入口を一本化する。
作業単位は子チケットに分割する。

### パスワードリセット導線の実装
- ステータス: 未着手
- 優先度: 高
- 担当者: 佐藤花子
- 開始日: 2026/08/11
- 期限日: 2026/08/18
- 見積工数: 8

**ASIS（現状）**
パスワードを忘れた場合、管理者による手動リセットしか手段がない。
問い合わせから復旧まで平均で1営業日かかっている。

**TOBE（あるべき姿）**
リセットメールの送信からパスワード再設定までをユーザー自身で完結できるようにする。
リセットリンクの有効期限は24時間とする。

### ログアウト時のセッション破棄
- ステータス: 未着手
- 優先度: 中
- 担当者: 山田太郎
- 開始日: 2026/08/19
- 期限日: 2026/08/21
- 見積工数: 4

**ASIS（現状）**
ログアウト後もリフレッシュトークンが有効なままで、再アクセスするとセッションが復活する。

**TOBE（あるべき姿）**
ログアウト時にサーバ側のセッションとリフレッシュトークンを確実に失効させ、
再アクセス時は必ずログイン画面へ戻るようにする。

## リリース手順書の更新
- ステータス: 未着手
- 優先度: 低
- 担当者: 山田太郎
- 開始日: 2026/08/11
- 期限日: 2026/08/14
- 見積工数: 2

**ASIS（現状）**
手順書にマイグレーション実行の記載が無く、実行タイミングが担当者ごとにばらついている。
前回のリリースでは実行漏れによる切り戻しが発生した。

**TOBE（あるべき姿）**
デプロイ手順にマイグレーション実行のステップを明記し、実行順序と確認方法を固定する。
`;

// ── AI用プロンプト ────────────────────────────────────────────────────────

export interface MdPromptContext {
  memberNames: string[];
  /** プロジェクトに登録済みの分類名 */
  categoryNames: string[];
  sprintName?: string;
  /** "2026/08/03" */
  today: string;
}

/**
 * AI（Claude 等）に渡して、そのまま取り込める .md を書いてもらうためのプロンプト。
 * 書式仕様・選択肢・プロジェクトのメンバー名と分類名を埋め込む。
 */
export function buildMdTicketPrompt(ctx: MdPromptContext): string {
  const members = ctx.memberNames.length > 0
    ? ctx.memberNames.join(" / ")
    : "（メンバー未設定。担当者の行は書かないこと）";
  const categories = ctx.categoryNames.length > 0
    ? ctx.categoryNames.join(" / ")
    : "（分類未設定。分類の行は書かないこと）";

  return `あなたはプロジェクト管理ツールに取り込むチケット一覧を Markdown で作成します。
以下の書式に厳密に従って出力してください。

# 出力のルール

- Markdown そのものを出力する。コードブロック（\`\`\`）で囲まない。前置きや解説も書かない。
- **見出しがチケットのタイトル**になる。
- 見出しの**直後**に \`- キー: 値\` 形式の箇条書きを置く。これがチケットの各項目になる。
- 項目の箇条書きは**連続した行**でまとめて書き、途中に空行や説明文を挟まない。
- 項目の箇条書きが終わったら**空行を1行入れてから**本文を書く。空行より後ろの文章・リスト・表は、すべてチケットの**詳細**になる。

# 詳細（本文）の書き方 ― 必須

チケットの本文には、**必ず ASIS（現状）と TOBE（あるべき姿）の両方**を書くこと。
対応内容だけを書いたチケットや、どちらか一方しか無いチケットは不可。**子チケットにも必ず両方書く。**

- **ASIS（現状）**: いま何がどうなっていて、何が問題なのかを書く。数値・エラー内容・発生条件など、読んだ人が状況を確認できる情報を含める。
- **TOBE（あるべき姿）**: 対応後にどうなっていればよいかを、完了したかどうか判定できる形で書く。

書式は次のとおり。**太字のラベル＋改行＋本文**で書き、ラベルの前後に空行を1行入れる。

**ASIS（現状）**
（現状と問題）

**TOBE（あるべき姿）**
（あるべき姿）

- **ラベルを見出し（\`#\` や \`###\`）で書かないこと。** \`###\` は子チケットとして解釈されてしまう。
- TOBE の後ろに補足（対象一覧・受入条件・注意点など）を続けて書いてよい。
- 情報が足りず ASIS が書けない場合は、推測で埋めずに「（要確認）」と明記する。

# 親チケットと子チケット

- 1つの作業で完結するものは \`##\` だけで書く（これが基本）。
- 複数の作業に分かれるもの（機能単位・改修一式など）だけ、\`##\` を親にして、その配下の作業を \`###\` の**子チケット**にする。
- \`###\` は必ず直前の \`##\` に属する。\`####\` 以降の階層は作らない（子は1階層まで）。
- **子チケットにも親と同じように項目を書く**。子だからといって省略しない。
- 迷ったら \`##\` だけで書く。無理に階層を作らない。

# 使えるキーと値

| キー | 値 |
|---|---|
| ステータス | ${MD_STATUS_LABELS.join(" / ")} |
| 優先度 | ${MD_PRIORITY_LABELS.join(" / ")} |
| 分類 | ${categories} |
| 担当者 | ${members} |
| 開始日 | YYYY/MM/DD 形式 |
| 期限日 | YYYY/MM/DD 形式 |
| 見積工数 | 時間数（半角の整数のみ。例: 4） |

- 上の7項目は**できる限りすべて埋める**。
- ただし**根拠なく推測しない**。判断材料が無い項目は、その行ごと省略する（空文字や「未定」とは書かない）。
- 上の表に無いキーは使わない（備考・目的などの独自キーも作らない。書きたいことは本文に書く）。
- 担当者・分類は上に挙げた名前のいずれかを完全一致で書く。該当が無ければその行を省略する。
${ctx.sprintName ? `\n# 対象スプリント\n\n${ctx.sprintName}\n` : ""}
# 今日の日付

${ctx.today}

# 出力例

${MD_TICKET_TEMPLATE}`;
}
