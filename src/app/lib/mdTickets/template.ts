// MDファイルからのチケット作成で使う、書き方の型（テンプレート）と AI 用プロンプト。
//
// テンプレートは1種類だけ。用途ごとに選ばせるより「全項目を埋めてもらう」1本に絞ったほうが
// 迷わず、取り込み後に画面側で直せばよいため。1本のなかに
// 「1作業で完結するチケット（##）」と「親＋子に分けるチケット（## + ###）」の
// 両方の例を入れてあり、どちらの書き方も同じテンプレートから出せるようにしている。
//
// 本文の項目構成（概要／対象画面／…／その他）も1セットに固定し、分類（バグ・機能追加など）に
// よる書き分けは「どこを厚く書くか」の指示としてプロンプト内に持たせる。UI で切り替えさせない。
import { MD_STATUS_LABELS, MD_PRIORITY_LABELS } from "./parse";

/** そのまま .md にして書き足せる記入例（全項目入り・フラット／親子の両方を含む） */
export const MD_TICKET_TEMPLATE = `## ログイン画面のバリデーション追加
- ステータス: 未着手
- 優先度: 高
- 分類: バグ
- 担当者: 山田太郎
- 開始日: 2026/08/03
- 期限日: 2026/08/10
- 見積工数: 4

**概要**
ログイン画面のメールアドレス欄に入力チェックが無く、未入力や不正な形式のまま送信できてしまう。
送信前にクライアント側で検証し、エラーを画面に表示する。

**対象画面**
ログイン画面（/login）

**対象機能**
メールアドレス入力欄のバリデーション　\`src/app/pages/LoginPage.tsx\`

**事象（ASIS）**
1. ログイン画面を開く
2. メールアドレスを空欄のままログインボタンを押す
3. サーバ側で500エラーになり、画面には何も表示されない

形式が不正な場合（\`test@\` や全角文字を含む場合）も同様に500になる。
発生頻度は100%。Chrome 141 / Windows 11 で確認。

**期待値（TOBE）**
送信前にメールアドレス形式を検証し、不正な場合は入力欄の直下にエラーメッセージを表示する。
未入力のままログインボタンを押せない状態にする。半角英数のみ許可し、全角は弾く。

**フロントエンド修正内容**
\`LoginPage.tsx\` に入力検証を追加し、エラー文言を入力欄の直下に表示する。
検証NGの間はログインボタンを disabled にする。

**受入条件**
- 記載の再現手順で500エラーが発生しないこと
- 空欄のときログインボタンが押せないこと
- \`test@\` を入力するとエラーメッセージが表示されること
- 全角文字を含むアドレスが弾かれること

**注意点**
サーバ側の検証は別チケットで対応する。本チケットはクライアント側のみを対象とする。

## ユーザー認証機能のリニューアル
- ステータス: 未着手
- 優先度: 高
- 分類: 機能追加
- 担当者: 山田太郎
- 開始日: 2026/08/11
- 期限日: 2026/08/28
- 見積工数: 20

**概要**
ログイン・パスワードリセット・ログアウトが機能ごとに個別実装されており、挙動が統一されていない。
認証の入口を一本化して作り直す。作業単位は子チケットに分割する。

**対象画面**
ログイン画面（/login）／パスワードリセット画面（/password-reset）／全画面共通のヘッダー

**対象機能**
認証まわり一式　\`src/app/contexts/AuthContext.tsx\` ほか

**事象（ASIS）**
パスワードリセットが未実装で、管理者による手動リセットしか手段がない。
ログアウトしてもリフレッシュトークンが有効なままで、再アクセスするとセッションが復活する。

**期待値（TOBE）**
ログイン・パスワードリセット・ログアウトが同じ認証基盤の上で動き、
セッションの生成と破棄が1か所で管理されている状態にする。

**影響範囲**
全画面（認証状態の判定を経由するため）。既存のログインセッションは維持する。

**受入条件**
- 子チケットがすべて完了していること
- 既存ユーザーが再ログインなしで引き続き利用できること

### パスワードリセット導線の実装
- ステータス: 未着手
- 優先度: 高
- 分類: 機能追加
- 担当者: 佐藤花子
- 開始日: 2026/08/11
- 期限日: 2026/08/18
- 見積工数: 8

**概要**
パスワードを忘れたユーザーが、自分でパスワードを再設定できるようにする。

**対象画面**
パスワードリセット申請画面（/password-reset）／パスワード再設定画面（/password-reset/:token）

**対象機能**
パスワードリセット　\`src/app/pages/PasswordResetPage.tsx\`（新規）

**事象（ASIS）**
パスワードを忘れた場合、管理者による手動リセットしか手段がない。
問い合わせから復旧まで平均で1営業日かかっている。

**期待値（TOBE）**
申請画面でメールアドレスを入力するとリセットメールが届き、メール内のリンクから
新しいパスワードを設定できる。リンクの有効期限は24時間とする。

**フロントエンド修正内容**
申請画面と再設定画面を新規作成する。再設定画面は開いた時点でトークンの有効性を検証する。

**バックエンド修正内容**
リセットメール送信APIと、トークン検証＋パスワード更新APIを追加する。

**受入条件**
- 登録済みメールアドレスを入力するとリセットメールが届くこと
- リンクから新しいパスワードを設定でき、そのパスワードでログインできること
- 24時間経過したリンクではエラーになること
- 未登録のメールアドレスを入力しても、登録の有無が判別できない応答になること
`;

/**
 * 既存チケットの配下へ子チケットを足すときの記入例。
 *
 * 親は既に決まっているので、この経路では階層を作らない（すべて \`##\` のフラットな一覧）。
 * 見出しは「親の中でやる1つの作業」の粒度で書く。
 */
export const MD_CHILD_TICKET_TEMPLATE = `## 申請画面の作成
- ステータス: 未着手
- 優先度: 高
- 分類: 機能追加
- 担当者: 山田太郎
- 開始日: 2026/08/03
- 期限日: 2026/08/06
- 見積工数: 8

**概要**
パスワードリセットの申請画面を新規に作成する。メールアドレスを入力して送信するところまでを担当する。

**対象画面**
パスワードリセット申請画面（/password-reset・新規）

**対象機能**
申請フォーム　\`src/app/pages/PasswordResetPage.tsx\`（新規）

**事象（ASIS）**
申請画面が存在せず、利用者は管理者に連絡するしか手段がない。

**期待値（TOBE）**
メールアドレスを入力して送信すると、リセットメール送信APIが呼ばれ、完了メッセージが表示される。
未登録のアドレスでも、登録の有無が分からない同じ文言を返す。

**フロントエンド修正内容**
申請画面を新規作成する。送信中はボタンを disabled にし、二重送信を防ぐ。

**受入条件**
- 登録済みアドレスを入力して送信すると完了メッセージが表示されること
- 未登録アドレスでも同じ文言が表示されること
- 送信中にボタンを連打しても2回送信されないこと

**注意点**
メール送信APIは別の子チケットで作成する。本チケットは画面のみを対象とする。

## リセットメール送信APIの作成
- ステータス: 未着手
- 優先度: 高
- 分類: 機能追加
- 開始日: 2026/08/03
- 期限日: 2026/08/07
- 見積工数: 6

**概要**
メールアドレスを受け取り、有効期限付きのリセットトークンを発行してメールを送信するAPIを作る。

**対象画面**
画面なし（API）

**対象機能**
リセットメール送信API　\`api/password-reset.ts\`（新規）

**事象（ASIS）**
トークンを発行する仕組みが無い。

**期待値（TOBE）**
リクエストを受けるとトークンを発行して保存し、リセットリンク入りのメールを送る。
有効期限は24時間とする。登録の有無に関わらず同じ応答を返す。

**バックエンド修正内容**
トークン発行・保存・メール送信を行うエンドポイントを追加する。

**DB・マイグレーション**
リセットトークンを保存するテーブルを追加する（トークン・ユーザーID・有効期限・使用済みフラグ）。

**受入条件**
- 登録済みアドレスへリセットメールが届くこと
- トークンが24時間で失効すること
- 未登録アドレスでも応答の内容と時間に差が出ないこと
`;

// ── AI用プロンプト ────────────────────────────────────────────────────────

export interface MdPromptContext {
  memberNames: string[];
  /** プロジェクトに登録済みの分類名 */
  categoryNames: string[];
  sprintName?: string;
  /** "2026/08/03" */
  today: string;
  /**
   * 既存チケットの配下へ子チケットを足す場合の親。
   * 指定すると「階層を作らず、すべて親の子として並べる」指示に切り替わる。
   */
  parentTicket?: { wbs: string; title: string };
}

/**
 * AI（Claude 等）に渡して、そのまま取り込める .md を書いてもらうためのプロンプト。
 * 書式仕様・本文の項目構成・分類ごとの書き分け・プロジェクトのメンバー名と分類名を埋め込む。
 */
export function buildMdTicketPrompt(ctx: MdPromptContext): string {
  const members = ctx.memberNames.length > 0
    ? ctx.memberNames.join(" / ")
    : "（メンバー未設定。担当者の行は書かないこと）";
  const categories = ctx.categoryNames.length > 0
    ? ctx.categoryNames.join(" / ")
    : "（分類未設定。分類の行は書かないこと）";

  // 既存チケットへ子を足す経路では親が決まっているため、階層を作らせない。
  const parent = ctx.parentTicket;
  const parentLabel = parent ? `${parent.wbs} ${parent.title}` : "";

  const intro = parent
    ? `あなたはプロジェクト管理ツールの既存チケット「${parentLabel}」に追加する**子チケットの一覧**を Markdown で作成します。`
    : "あなたはプロジェクト管理ツールに取り込むチケット一覧を Markdown で作成します。";

  const headingRules = parent
    ? `- **見出しがチケットのタイトル**になる。見出しは**すべて \`##\`**（同じ階層）で書く。
- 書いた \`##\` は**すべて「${parentLabel}」の子チケット**として登録される。親チケットは新しく作られない。
- **\`###\` 以降の階層は使わない。**子チケットの下にさらに子は作れない（階層は1段までのため）。`
    : "- **見出しがチケットのタイトル**になる。";

  return `${intro}
以下の書式に厳密に従って出力してください。

# 出力のルール

- Markdown そのものを出力する。コードブロック（\`\`\`）で囲まない。前置きや解説も書かない。
${headingRules}
- 見出しの**直後**に \`- キー: 値\` 形式の箇条書きを置く。これがチケットの各項目になる。
- 項目の箇条書きは**連続した行**でまとめて書き、途中に空行や説明文を挟まない。
- 項目の箇条書きが終わったら**空行を1行入れてから**本文を書く。空行より後ろが、そのチケットの**詳細**になる。

# 使えるキーと値

| キー | 値 |
|---|---|
| ステータス | ${MD_STATUS_LABELS.join(" / ")} |
| 優先度 | ${MD_PRIORITY_LABELS.join(" / ")} |
| 分類 | ${categories} |
| 担当者 | ${members} |
| 開始日 | YYYY/MM/DD 形式 |
| 期限日 | YYYY/MM/DD 形式 |
| 見積工数 | 時間数（半角の整数のみ。例: 4） |

- 上の7項目は**できる限りすべて埋める**。
- ただし**根拠なく推測しない**。判断材料が無い項目は、その行ごと省略する（空文字や「未定」とは書かない）。
- 上の表に無いキーは使わない（備考・目的などの独自キーも作らない。書きたいことは本文に書く）。
- 担当者・分類は上に挙げた名前のいずれかを完全一致で書く。該当が無ければその行を省略する。

# 詳細（本文）の書き方 ― 必須

本文は決まった項目構成で書く。ラベルは**太字で単独行**に置き、**その直後で必ず改行して**内容を書く。
ラベルと内容を同じ行に続けない。項目と項目の間には空行を1行入れる。

**概要**
（内容）

**対象画面**
（内容）

順序は下表のとおり固定する。「必須」の項目は必ず書く。「該当時」の項目は、該当が無ければ
**項目ごと省略**する（ラベルだけ置いて「なし」と書かない）。

| 順 | ラベル | 要否 | 書く内容 |
|---|---|---|---|
| 1 | 概要 | 必須 | このチケットで何をするのかを2〜3行で。最初に読む要約 |
| 2 | 対象画面 | 必須 | 画面名・URL・該当箇所。画面に依らない作業は「画面なし（バッチ / API / ドキュメント 等）」と書く |
| 3 | 対象機能 | 必須 | 機能名・関数名・ファイルパスなど、触る対象を特定できる情報 |
| 4 | 事象（ASIS） | 必須 | いま何がどうなっていて、何が問題なのか。確認できた事実だけを書く |
| 5 | 期待値（TOBE） | 必須 | 対応後にどうなっていればよいか |
| 6 | フロントエンド修正内容 | 該当時 | 画面・コンポーネント・状態管理の変更点 |
| 7 | バックエンド修正内容 | 該当時 | API・サーバー処理の変更点 |
| 8 | DB・マイグレーション | 該当時 | テーブル・カラム・権限・関数の変更点と投入順序 |
| 9 | インフラ・サーバー設定修正内容 | 該当時 | 環境変数・デプロイ設定・権限・外部サービス設定 |
| 10 | 影響範囲 | 該当時 | 他の画面・機能・既存データへの波及 |
| 11 | 受入条件 | 必須 | 完了と判断する条件を箇条書きで。1行1条件、検証できる書き方にする |
| 12 | 注意点 | 該当時 | 実装時の落とし穴、既知の制約、作業順序の依存 |
| 13 | その他 | 該当時 | 参考リンク、関連チケット、決定事項の補足 |

- ラベルを**見出し（\`#\` \`##\` \`###\` \`####\`）で書かないこと**。見出しは別のチケットとして解釈される。
- **子チケットにも同じ構成で書く。**子だからといって省略しない。
- 分からない項目は推測で埋めず「（要確認）」と明記する。

# 分類ごとの書き分け

項目構成は共通のまま、**分類の値に応じてどこを厚く書くかを変える**。
分類名が下記と完全に一致しなくても、意味が近いものとして扱う。

**バグ・不具合**
- 事象（ASIS）に、再現手順（番号付き）／発生条件／エラーメッセージ／発生頻度／確認した環境を必ず含める
- 原因の推定は事象に書かず「注意点」に回す。事象には確認できた事実だけを書く
- 受入条件の先頭に「記載の再現手順で再現しないこと」を入れる

**機能追加・新規開発**
- 概要に、なぜ必要か（背景・目的）を書く
- 期待値（TOBE）に仕様を漏れなく書く。画面項目・入力制限・エラー時の挙動まで含める
- 受入条件は期待値の各項目と1対1で対応させる
- フロントエンド／バックエンド／DB・マイグレーションのうち、触るものは必ず書く

**改修・改善・リファクタリング**
- 事象（ASIS）に現状の課題を数値で書く（処理時間・件数・エラー率など）
- 期待値（TOBE）には目標値を書く
- **影響範囲を必ず書く。**利用者から見た挙動が変わらないことを担保する範囲を明示する

**仕様確認・調査・検討**
- 事象（ASIS）に「何が分かっていないか」「なぜ判断できないか」を書く
- 期待値（TOBE）に「何が決まれば完了か」を書く
- 受入条件は「決定内容が記録されていること」など、成果物で判定できる形にする
- 修正内容の項目（フロントエンド／バックエンド／DB／インフラ）は省略してよい

**運用・保守・作業**
- 手順と実施順序を「注意点」に書く
- 影響範囲と、失敗したときの切り戻し手順を必ず書く

上のどれにも当てはまらない分類のときは、共通の構成のまま書く。

${parent ? `# 子チケットの粒度

- 見出し1つ＝「${parent.title}」の中でやる**1つの作業**。担当者を割り当てて着手できる単位まで分ける。
- 親チケットの内容をそのまま1件に書き写さない。分けられないなら子チケットは作らない。
- **1件ずつに、親と同じ項目・同じ本文構成で書く**。子だからといって省略しない。
- 作業の順序に依存関係があるときは「注意点」にその旨を書く。

# 追加先

既存チケット **${parentLabel}** の子チケットとして登録される。
登録先のスプリントは親チケットと同じになるので、自分で選ばない。
` : `# 親チケットと子チケット

- 1つの作業で完結するものは \`##\` だけで書く（これが基本）。
- 複数の作業に分かれるもの（機能単位・改修一式など）だけ、\`##\` を親にして、その配下の作業を \`###\` の**子チケット**にする。
- \`###\` は必ず直前の \`##\` に属する。\`####\` 以降の階層は作らない（子は1階層まで）。
- **子チケットにも親と同じように項目と本文を書く**。子だからといって省略しない。
- 迷ったら \`##\` だけで書く。無理に階層を作らない。
${ctx.sprintName ? `\n# 対象スプリント\n\n${ctx.sprintName}\n` : ""}`}
# 今日の日付

${ctx.today}

# 出力例

${parent ? MD_CHILD_TICKET_TEMPLATE : MD_TICKET_TEMPLATE}`;
}
